import { Card } from "@mui/material";

const InfoCard = () => {
  return (
    <Card
      variant="elevation"
      sx={{
        padding: 4,
      }}
    >
      Failed to fetch the data.
    </Card>
  );
};

export { InfoCard };
